import React from "react";

const Pedidos = () => {
  const styles = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
    backgroundColor: "#fff",
    color: "#000",
    fontSize: "24px",
  };

  return <div style={styles}>Em desenvolvimento</div>;
};

export default Pedidos;
